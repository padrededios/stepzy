version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: futsal-postgres
    environment:
      POSTGRES_USER: futsal
      POSTGRES_PASSWORD: futsal_dev_2024
      POSTGRES_DB: futsal_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: futsal-redis
    ports:
      - "6379:6379"
    restart: unless-stopped

volumes:
  postgres_data: